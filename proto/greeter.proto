syntax = "proto3";

package greeter;

// GreeterServiceというサービスを定義
service GreeterService {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// クライアントから送るリクエストメッセージ
message HelloRequest {
  string name = 1;
}

// サーバーから返すレスポンスメッセージ
message HelloReply {
  string message = 1;
}
